spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/logimatch
    driver-class-name: org.postgresql.Driver
    username: logimatch
    password: logimatch123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    locations: classpath:db/migration
